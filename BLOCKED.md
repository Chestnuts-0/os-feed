# BLOCKED.md — 待裁决清单（P0a 卡片排版根治）

## 1. 任务书三死规矩冲突的决策记录（任务 1，已按 skill 原则处理，待栗子裁决确认）

任务书同时要求：
- A. 校验线：缓存命中的存量短卡也要校验 → 重评（死规矩「否则存量短卡全部进 pending 下轮才补评，本轮 849 张全丢」）
- B. 既有测试「只增不减回归」锁定「历史卡缓存命中零重评」（LLM mock 直接 throw，断言零调用）
- C. 现有测试禁改 + 验收「230 passed + 3 基线失败，无新增」

A 与 B 不可调和（A 会让 12 个既有测试新增失败，违反 C）。

**执行决策**（按 skill「任务书内部矛盾 → 以验收全过为准 + 书面记录」）：
1. **缓存命中的存量卡豁免长度校验**，原样输出——保住「历史卡缓存命中零重评」铁律（增量管道零 LLM 成本设计）与既有测试。存量短卡由任务 2 清 reasonCn 失效后走新评分路径收敛，**最终状态不受影响**（收敛后缓存命中的卡全部达标，后续增量不再遇到不达标存量卡）
2. **重评失败 + detail 兜底也失败**（detail 异常短）→ 保留原评分进 feed + 警告日志——任务书未规定此分支（只规定「有 detailMap 则 detail 兜底、无则 pending」），补「不丢卡」默认（符合只增不减设计）。真实数据 detail 全库 min 125 字，此分支几乎不触发
3. scoreBatched 内部缺失补评恢复旧语义（1 次、不校验）——长度校验统一在组装循环做（避免双重校验重复调用 LLM）

**请栗子确认**：以上三个偏差是否认可。

## 2. 任务 2 执行观察（无需裁决，仅供知悉）

- agnès 重评阶段空响应/JSON 截断故障率明显高于排查时（实测 6/10 一次达标，本轮重评多数卡 3 次全败后走 detail 兜底）
- detail 兜底 100% 成功（已兜底 40+ 张，effLen 100-130.5 全部达标），机制按设计工作，数据收敛不受影响，仅耗时偏长

## 3. 其他

- 无（截至本版）
